import { StyledIconButton } from "@/shared/ui/styled/iconButton";
import { IconButtonProps } from "@/shared/ui/types/iconButton";

export const IconButton = (props: IconButtonProps) => {
  return <StyledIconButton {...props} />;
};
